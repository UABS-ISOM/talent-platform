type Query {
  _: Boolean
  me: User
}

type Mutation {
  _: Boolean
  addCourse(name: String!, description: String!): Course!
  editMe(input: EditMeInput!): User!
}

input EditMeInput {
  name: String
  pronouns: String
  overview: String
  skills: [String!]
}

scalar Date
